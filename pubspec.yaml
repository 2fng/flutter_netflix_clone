name: flutter_movie_db
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Icons
  cupertino_icons: ^1.0.2
  ionicons: ^0.2.2

  # Networking
  retrofit: ^4.0.1
  dio: ^5.0.1

  # State Management
  flutter_bloc: ^8.1.2

  # Others
  get_it: ^7.2.0
  equatable: ^2.0.5
  flutter_svg: "2.0.3"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.0
  json_serializable: ^6.5.4
  retrofit_generator: ^5.0.0


flutter:
  uses-material-design: true
  assets: 
    - assets/images/
